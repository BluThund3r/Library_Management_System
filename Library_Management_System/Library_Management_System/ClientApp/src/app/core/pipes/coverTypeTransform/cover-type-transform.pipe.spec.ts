import { CoverTypeTransformPipe } from './cover-type-transform.pipe';

describe('CoverTypeTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new CoverTypeTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
